# 1장 근접성 서비스

## 근접성 서비스란?

근접성 서비스(Proximity Service)는 사용자의 현재 위치를 기준으로 **일정 반경 내에 존재하는 사업장이나 시설을 검색하는 서비스**다.

대표적인 예는 네이버 지도나 카카오맵에서

> "현재 위치에서 500m 이내 음식점 찾기"

같은 기능이다.

핵심 문제는 **수억 개의 장소 중 현재 위치와 가까운 장소를 빠르게 찾아내는 것**이다.

---

## 1단계. 문제 이해 및 설계 범위

### 기능적 요구사항

* 사용자 위치 기준 일정 반경 내 사업장 검색
* 최대 검색 반경 20km
* 사용자가 검색 반경 변경 가능
* 사업장 소유주가 사업장 정보 추가/수정/삭제 가능
* 사업장 정보 변경은 실시간 반영할 필요 없음

    * 다음날까지 반영되면 충분
* 사용자가 이동할 때마다 검색 결과를 실시간 갱신할 필요 없음

### 비기능적 요구사항

**낮은 응답 지연**

주변 사업장 검색은 사용자 인터랙션과 직접 연결되므로 빠른 응답이 필요하다.

**고가용성과 확장성**

점심시간의 강남역처럼 특정 시간과 지역에 트래픽이 집중될 수 있으므로 이를 감당할 수 있어야 한다.

**개인정보 보호**

위치 정보는 민감 정보이므로 저장 및 전송 시 보호가 필요하다.

### 규모 추정

* DAU: 1억 명
* 사업장: 2억 개
* 사용자당 하루 검색: 5회

```text
하루 요청 수 = 100M × 5 = 500M

평균 QPS = 500M / 86,400 ≈ 5,800
```

따라서 평균적으로 약 **5~6K QPS**가 발생한다.

실제 시스템에서는 특정 시간이나 지역에 요청이 몰리므로 Peak QPS는 이보다 훨씬 높을 수 있다.

---

## 2단계. 개략적 설계

전체 시스템은 크게 두 서비스로 나눈다.

### LBS(Location Based Service)

사용자의 위치와 반경을 받아 주변 사업장을 검색한다.

* 거의 Read 요청
* 높은 QPS
* 무상태 서비스
* 수평 확장이 쉬움

### Business Service

사업장 정보를 관리한다.

* 사업장 등록/수정/삭제
* 사업장 상세 조회
* 쓰기 QPS는 상대적으로 낮음

두 서비스 모두 Stateless하게 구성하여 트래픽 증가 시 서버를 수평 확장할 수 있다.

### API

주변 검색:

```http
GET /v1/search/nearby?latitude=&longitude=&radius=
```

사업장 관리:

```http
GET    /v1/businesses/:id
POST   /v1/businesses
PUT    /v1/businesses/:id
DELETE /v1/businesses/:id
```


### 주변 사업장을 어떻게 찾을까?

단순하게 생각하면 DB에 다음과 같이 사업장의 위도와 경도를 저장할 수 있다.

```text
business
----------------
id
latitude
longitude
...
```

하지만 일반적인 B-Tree 인덱스는 기본적으로 **1차원 데이터를 검색하는 구조**다.

우리가 원하는 것은

> 특정 `(latitude, longitude)`를 중심으로 반경 500m 안의 데이터

라는 **2차원 공간 검색**이다.

따라서 지도를 작은 영역으로 나누고 해당 영역을 빠르게 검색할 수 있도록 만드는 **지리 공간 인덱스(Spatial Index)**가 필요하다.

대표적인 방법은 다음과 같다.

| 방식        | 특징                                |
| --------- | --------------------------------- |
| 균등 격자     | 동일 크기의 격자로 지도 분할                  |
| Geohash   | 2차원 위치를 문자열로 변환                   |
| Quadtree  | 데이터 밀도에 따라 공간을 재귀적으로 분할           |
| Google S2 | 구면을 Cell로 분할하고 Hilbert Curve 이용   |
| R-tree    | 공간 객체를 Bounding Rectangle 기반으로 색인 |

실무에서는 PostGIS처럼 DB에서 제공하는 Spatial Index를 활용할 수도 있다.


### Geohash

Geohash는 **2차원의 위도/경도 데이터를 1차원 문자열로 변환**한다.

세계를 반복적으로 작은 영역으로 나누면서 위치를 표현하고, 최종적으로 보통 Base32 문자열로 나타낸다.

```text
위도 + 경도

        ↓

wydm6...
```

문자열이 길수록 더 작은 영역을 의미하므로 위치를 더 정밀하게 표현한다.

#### 장점

* 구현과 사용이 비교적 단순함
* 별도의 트리 구축이 필요 없음
* DB / Key-Value Store와 함께 사용하기 편함
* 사업장 추가/삭제에 따른 인덱스 갱신이 쉬움

#### 문제: 격자 경계

Geohash에서 주의해야 할 점은 **격자 경계 문제**다.

공간적으로 아주 가까운 두 사업장이 다음처럼 서로 다른 격자에 존재할 수 있다.

```text
┌────────┬────────┐
│    A   │ B      │
└────────┴────────┘
```

A와 B의 실제 거리는 매우 가까워도 서로 다른 Geohash를 가질 수 있다.

즉,

> 공통 Prefix가 길면 두 위치가 가까울 가능성은 높지만, 두 위치가 가깝다고 해서 반드시 같은 Prefix를 가지는 것은 아니다.

따라서 현재 위치가 포함된 Geohash만 검색해서는 안 되고 **주변의 인접 Cell까지 함께 검색해야 한다.**


### Quadtree

Quadtree는 2차원 공간을 네 개 영역으로 반복해서 분할하는 자료구조다.

```text
┌─────────┐
│    │    │
│────┼────│
│    │    │
└─────────┘
```

예를 들어 다음과 같은 조건을 사용할 수 있다.

> 한 Cell에 사업장이 100개 이하가 될 때까지 분할한다.

그러면 강남처럼 사업장이 많은 지역은 작은 Cell로 세밀하게 나뉘고, 산이나 바다처럼 사업장이 적은 곳은 큰 Cell로 남는다.

즉, **데이터 밀도에 따라 Cell 크기가 달라진다.**

#### Leaf Node

Quadtree에서 Leaf Node는 **더 이상 분할되지 않은 최종 영역**을 의미한다.

예를 들어 Leaf 하나에 최대 100개의 사업장을 저장하도록 했다면:

```text
전체 사업장 n개
       ↓
    Quadtree
       ↓
┌──────┬──────┬──────┐
│ Leaf │ Leaf │ Leaf │ ...
│≤100개│≤100개│≤100개│
└──────┴──────┴──────┘
```

전체 사업장이 2억 개라면 Leaf 수는 대략 다음 정도로 추정할 수 있다.

```text
200M / 100 ≈ 2M
```

실제로 모든 Leaf가 정확히 100개의 사업장을 가지는 것은 아니므로 이는 대략적인 규모 추정이다.

#### 장점

Quadtree의 가장 큰 장점은 **데이터 밀도에 따라 공간 크기를 동적으로 조절할 수 있다는 것**이다.

따라서

> 가장 가까운 K개의 사업장

과 같은 검색에도 적합하다.

#### 단점

Geohash와 달리 트리를 직접 구축하고 관리해야 한다.

사업장이 추가되거나 삭제되면:

```text
root
 ↓
node
 ↓
node
 ↓
leaf
```

루트부터 해당 Leaf까지 트리를 탐색해야 한다.

균형 잡힌 트리를 가정하면 탐색 비용은 대략 `O(log n)`으로 볼 수 있다.

또한 여러 스레드가 동시에 트리를 변경한다면 Lock을 고려해야 하며, 필요에 따라 트리 구조를 다시 조정하는 작업도 필요할 수 있다.



### Geohash vs Quadtree

|           | Geohash    | Quadtree      |
| --------- | ---------- | ------------- |
| 구현        | 쉬움         | 상대적으로 복잡      |
| 구조 구축     | 필요 없음      | 필요            |
| 반경 검색     | 가능         | 가능            |
| K Nearest | 상대적으로 불편   | 유리            |
| Cell 크기   | 정밀도에 따라 고정 | 데이터 밀도에 따라 변경 |
| 갱신        | 쉬움         | 상대적으로 어려움     |
| 동시성       | 단순         | Lock 고려 필요    |

면접에서는 둘 중 하나를 선택하고 **왜 해당 방식을 선택했는지를 설명하는 것이 중요하다.**

이 시스템처럼

> 주변 N km 사업장 검색 + 사업장 정보 변경

정도의 요구사항이라면 **Geohash가 비교적 구현과 설명이 단순한 선택지**다.



### Google S2

Google S2는 지구를 여러 개의 Cell로 분할하여 위치를 표현하는 공간 라이브러리다.

Hilbert Curve를 이용해 공간 정보를 1차원으로 변환하면서 **공간적으로 가까운 지점의 Locality를 최대한 유지**한다.

Geohash보다 유연한 영역 표현이 가능하고 Geofencing 같은 기능에도 적합하다.

다만 내부 구조가 복잡하기 때문에 시스템 설계 면접에서는 Geohash나 Quadtree가 설명하기 쉽다.

---

## 3단계. 상세 설계

### 사업장 데이터베이스

사업장은 약 2억 개 존재한다.

사업장 정보 자체가 하나의 DB에서 처리하기 부담스러운 규모라면 다음처럼 Business ID를 기준으로 샤딩할 수 있다.

```text
hash(business_id) → shard
```

Business ID 기반 샤딩은 데이터와 부하를 여러 샤드에 비교적 균등하게 분산할 수 있고 운영하기도 상대적으로 단순하다.



### Geohash Index

사업장 데이터와 별도로 지리 정보 검색을 위한 인덱스를 둔다.

```text
geohash | business_id
---------------------
wydm6   | 123
wydm6   | 456
wydm7   | 789
```

`(geohash, business_id)`를 복합키로 사용할 수 있다.

반대로 하나의 Geohash에 속하는 사업장 ID를 JSON 배열로 저장하는 방법도 있다.

```text
wydm6 → [123, 456, 789, ...]
```

하지만 이 방식은 사업장 하나를 추가하거나 삭제할 때 배열 전체를 읽어 수정해야 한다.

따라서 **사업장 하나당 Row 하나를 두는 방식이 개별 사업장의 추가/삭제를 처리하기 쉽다.**


### 지리 정보 인덱스의 확장

중요한 점은

> 데이터가 많다고 무조건 샤딩할 필요는 없다.

는 것이다.

지리 정보 인덱스 자체의 데이터 크기가 단일 DB에 수용 가능한 수준이라면 **데이터 크기만을 이유로 성급하게 샤딩할 필요는 없다.**

오히려 이 시스템에서는 저장 공간보다 높은 읽기 QPS와 네트워크 대역폭이 먼저 병목이 될 수 있다.

이 경우 Read Replica를 추가해 읽기 요청을 분산하고 전체 읽기 처리량을 확장할 수 있다.



### Cache

사업장 정보는 상대적으로 자주 변경되지 않으므로 캐시하기 좋다.

캐시는 크게 두 가지 정보를 저장할 수 있다.

```text
1. Geohash → Business IDs
2. Business ID → Business 상세 정보
```

### 사용자 위치를 Cache Key로 사용하지 않는 이유

사용자의 휴대전화에서 얻는 GPS 값은 정확히 고정되어 있지 않으며 사용자가 조금만 이동해도 계속 변한다.

```text
37.497931
37.497932
37.497928
...
```

위도와 경도 자체를 Cache Key로 사용하면 동일한 지역에서 검색하더라도 계속 다른 Key가 생성되어 Cache Hit율이 떨어진다.

반면 Geohash를 사용하면 같은 Cell 안에 있는 사용자들은 동일한 Key를 사용할 수 있다.

```text
geo:wydm6 → [123, 456, 789, ...]
```

따라서 공간을 일정 단위로 묶어 캐시할 수 있어 캐시 효율을 높일 수 있다.


### 실제 주변 사업장 검색 흐름

예를 들어 사용자가 **현재 위치에서 500m 이내 음식점**을 검색한다고 하자.

#### 1. 요청

클라이언트가 다음 정보를 LBS에 전달한다.

```text
latitude
longitude
radius = 500m
```

#### 2. Geohash 정밀도 계산

LBS는 검색 반경 500m를 처리하기 적절한 Geohash Precision을 선택한다.

검색 반경이 작으면 더 높은 정밀도를, 검색 반경이 크면 더 낮은 정밀도를 사용할 수 있다.

#### 3. 인접 Cell 계산

현재 위치가 포함된 Cell뿐 아니라 **Geohash의 격자 경계 문제를 해결하기 위해 주변 Cell도 함께 구한다.**

#### 4. 사업장 ID 검색

각 Geohash에 대해 Redis 등에서 다음 정보를 조회한다.

```text
geohash → business IDs
```

여러 Cell에 대한 조회를 병렬적으로 수행하면 검색 Latency를 줄일 수 있다.

#### 5. 사업장 상세 정보 조회

얻어진 Business ID를 이용해 사업장 상세 정보를 가져온다.

```text
business_id → business info
```

해당 정보 역시 Redis 등에 캐시할 수 있다.

#### 6. 정확한 거리 계산

Geohash는 어디까지나 **검색해야 할 후보군을 빠르게 줄이는 공간 인덱스**다.

Geohash Cell 자체가 정확한 원형 검색 범위를 의미하는 것은 아니다.

따라서 후보 사업장 각각에 대해 사용자와의 실제 거리를 계산한다.

```text
전체 2억 개 사업장
        ↓
     Geohash
        ↓
후보 사업장 빠르게 추출
        ↓
  실제 거리 계산
        ↓
반경 밖 사업장 제거
        ↓
    거리순 정렬
        ↓
      반환
```

즉,

> **Spatial Index는 정확한 거리 계산을 대신하는 것이 아니라, 정확한 거리 계산을 수행해야 할 후보군을 빠르게 줄이기 위한 장치다.**



### 사업장 정보 변경

사업장 정보는 실시간으로 검색 결과에 반영할 필요가 없다는 요구사항이 있다.

따라서 사업장 변경 사항은 우선 Primary DB에 기록한다.

```text
Business Service
       ↓
   Primary DB
       ↓
  Batch / Sync
       ↓
Geo Index / Cache
```

검색용 인덱스와 캐시는 비동기로 갱신할 수 있다.

요구사항이 "다음날까지 반영"이므로 야간 배치를 사용해도 요구사항을 만족한다.

향후 실시간 반영이 필요해진다면 CDC나 Kafka 등을 이용한 이벤트 기반 갱신 구조로 확장할 수도 있다.



### 영업시간 / 사업장 유형 검색

예를 들어 사용자가

> 500m 이내에서 **현재 영업 중인 카페**

를 찾는다고 하자.

이 경우 Geohash 자체에 사업장 유형이나 영업시간 조건까지 포함할 필요는 없다.

먼저 공간 인덱스를 이용해 주변 후보군을 줄인다.

```text
전체 2억 사업장
       ↓
    Geohash
       ↓
주변 사업장 200개
       ↓
category = CAFE
open = true
       ↓
      30개
```

그 후 Business Metadata를 이용해 영업시간이나 사업장 유형을 필터링한다.

공간 검색을 통해 이미 후보군이 충분히 작아졌기 때문에 후처리 비용도 줄어든다.



### 지역 분산

서비스가 글로벌하게 제공된다면 사용자와 시스템 사이의 물리적 거리를 줄이기 위해 여러 Region을 사용할 수 있다.

```text
한국 사용자 → Asia Region
미국 사용자 → US Region
유럽 사용자 → EU Region
```

각 지역에 LBS와 Redis 등을 배치하면 네트워크 Latency를 줄일 수 있다.

또한 위치 정보는 개인정보 규제의 영향을 크게 받으므로 **Data Residency와 지역별 개인정보 보호법**도 함께 고려해야 한다.



### 최종 아키텍처

논리적으로 다음과 같은 구조로 구성할 수 있다.

```text
                           ┌─────────────────┐
                           │  Load Balancer  │
                           └────────┬────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
             /search/nearby                    /businesses
                    │                               │
             ┌──────▼──────┐               ┌────────▼────────┐
             │     LBS     │               │ Business Service│
             └──────┬──────┘               └────────┬────────┘
                    │                               │
                    │                               │ read/write
                    │                               ▼
                    │                       ┌─────────────────┐
                    │                       │   Business DB   │
                    │                       └────────┬────────┘
                    │                                │
          ┌─────────┴─────────┐              Batch / Async Sync
          │                   │                     │
          ▼                   ▼                     ▼
┌─────────────────┐  ┌─────────────────┐   ┌─────────────────┐
│    Geo Cache    │  │ Business Cache  │   │   Geo Index     │
│ Geohash → IDs   │  │ ID → Business   │   │ Geohash → IDs   │
└─────────────────┘  └─────────────────┘   └─────────────────┘
```

여기서 Geo Cache와 Business Cache는 **논리적인 역할의 구분**이며 반드시 물리적으로 서로 다른 Redis 클러스터를 사용해야 한다는 의미는 아니다.

---

## 이 장에서 가져갈 핵심

이 장의 핵심은 Geohash 알고리즘 자체를 외우는 것보다 **2차원 공간 검색 문제를 대규모 시스템에서 어떻게 효율적으로 처리할 것인가**에 있다.

일반적인 방식으로 모든 사업장의 거리를 계산하면:

```text
2억 개 사업장
      ↓
각 사업장과 거리 계산
```

검색 비용이 너무 크다.

따라서 다음과 같이 문제를 바꾼다.

```text
2차원 공간
    ↓
Spatial Index
    ↓
검색 대상 Cell 결정
    ↓
후보 사업장 ID 추출
    ↓
정확한 거리 계산
    ↓
필터링 / 정렬
```

즉, **Spatial Index를 이용해 전체 데이터 중 실제 거리 계산이 필요한 후보군을 빠르게 줄이는 것이 핵심**이다.

그리고 대규모 트래픽을 처리하기 위해 다음 요소들을 조합한다.

```text
Spatial Index
      +
Stateless LBS
      +
Redis Cache
      +
Business DB
      +
Read Replica
      +
Multi Region
```

면접 관점에서는 특히 다음 흐름을 설명할 수 있으면 좋다.

1. 일반적인 B-Tree만으로 2차원 공간 검색을 효율적으로 처리하기 어려운 이유
2. Geohash가 2차원 위치를 어떻게 검색 가능한 형태로 변환하는지
3. Geohash의 격자 경계 문제와 인접 Cell 조회가 필요한 이유
4. Spatial Index로 후보군을 줄인 후 실제 거리를 계산하는 이유
5. Geohash와 Quadtree의 차이와 각각의 Trade-off

결국 이 장의 전체 흐름은 다음 한 줄로 정리할 수 있다.

> **2차원 공간 → Spatial Index → 후보군 축소 → 실제 거리 계산 → 필터링 및 정렬**
