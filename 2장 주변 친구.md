# 2. 주변 친구

## 1. 요구사항

### 기능 요구사항

- 사용자는 모바일 앱에서 주변 친구를 확인할 수 있다.
- 친구와의 거리를 확인할 수 있다.
- 친구 위치가 마지막으로 갱신된 시간을 확인할 수 있다.
- 주변 친구 목록은 몇 초 단위로 갱신되어야 한다.

### 비기능 요구사항

- **Low Latency**
  - 친구의 위치 변경이 빠르게 반영되어야 한다.

- **Reliability**
  - 안정적인 시스템이어야 하지만 일부 위치 이벤트 유실 정도는 허용할 수 있다.

- **Eventual Consistency**
  - 위치 정보는 반드시 즉시 모든 서버에 동일하게 반영될 필요는 없다.
  - 몇 초 정도의 차이는 허용 가능하다.

여기서 중요한 점은 **위치 정보에 Strong Consistency까지 요구할 필요가 없다는 것**이다.

예를 들어 친구가 실제 위치보다 2~3초 전 위치에 표시된다고 해서 서비스 전체가 실패하는 것은 아니다.

---

## 2. 규모 추정

책에서는 다음 정도의 규모를 가정한다.

- DAU: 약 1억 명
- 동시 사용자: 약 1,000만 명
- 위치 갱신 주기: 30초
- 평균 친구 수: 약 400명
- 주변 친구 판단 거리: 약 8km

따라서 위치 업데이트 요청은

```text
10,000,000 / 30초
≈ 334,000 updates/sec
```

즉 서버는 초당 약 **33만 건의 위치 업데이트**를 처리해야 한다.

그런데 더 큰 문제는 위치를 받는 것이 아니다.

한 사용자의 위치가 변경되면 해당 사용자의 여러 친구에게 다시 전달해야 한다.

예를 들어 평균 친구 400명 중 10% 정도가 온라인 상태이면서 관련된 사용자라고 가정하면

```text
334,000 × 400 × 10%
≈ 14,000,000
```

초당 약 **1,400만 건의 위치 변경 메시지​**를 전달해야 할 수 있다.

### 배운 점

대규모 시스템에서

```text
클라이언트 → 서버 요청량
```

만 보고 시스템 부하를 판단하면 안 된다.

한 번의 요청이 서버 내부에서 **fan-out** 되면서 훨씬 많은 작업을 만들어낼 수 있다.

이번 문제의 진짜 병목도 위치 입력보다는

```text
한 사용자의 위치 변경
        ↓
여러 친구에게 위치 변경 전파
```

에서 발생한다.

---

## 3. 전체 아키텍처

대략적인 구조는 다음과 같다.

```text
                 ┌──────────────┐
                 │ Load Balancer│
                 └──────┬───────┘
                        │
            ┌───────────┴───────────┐
            │                       │
            ▼                       ▼
     REST API Server         WebSocket Server
            │                       │
            │                       │
      User / Friend DB        Redis Pub/Sub
                                    │
                                    │
                              Location Cache
                                    │
                              Redis / Storage
                                    │
                            Location History DB
```

핵심 구성 요소는 크게 다음과 같다.

### REST API Server

일반적인 Request/Response 작업을 처리한다.

예를 들면

- 친구 추가
- 친구 삭제
- 사용자 프로필 변경
- 친구 목록 조회

등이다.

이 서버들은 상태를 가지고 있을 필요가 없기 때문에 **Stateless**하게 구성하여 쉽게 Scale-out 할 수 있다.

---

## 4. WebSocket을 사용하는 이유

친구 위치는 계속 변경된다.

HTTP Polling으로 구현하면

```text
Client

5초마다
GET /nearby-friends

5초마다
GET /nearby-friends

5초마다
GET /nearby-friends
```

처럼 반복해서 서버에 요청해야 한다.

사용자가 수백만 명이면 불필요한 요청이 엄청나게 많아진다.

그래서 서버와 클라이언트 사이에 **WebSocket 연결을 유지**한다.

```text
Client
   │
   │ WebSocket
   │
WebSocket Server
```

친구 위치가 변경되었을 때만

```text
친구 위치 변경
      ↓
WebSocket Server
      ↓
Client
```

형태로 Push 한다.

### 배운 점

실시간 서비스라고 무조건 WebSocket을 사용하는 것이 아니라,

> **서버에서 클라이언트로 지속적으로 이벤트를 전달해야 하는가?**

를 기준으로 판단해야 한다.

이번 시스템에서는 친구 위치 변경 이벤트가 계속 발생하기 때문에 WebSocket이 적합하다.

---

## 5. Redis Location Cache

현재 사용자의 위치는 Redis에 저장한다.

예를 들어

```text
user:12345

latitude  = 37.4979
longitude = 127.0276
timestamp = ...
```

형태이다.

DB가 아니라 Redis를 사용하는 이유는 위치가 매우 자주 변경되기 때문이다.

```text
30초마다 위치 갱신
        ↓
초당 약 334,000건
```

현재 위치는 읽기/쓰기가 매우 빈번하기 때문에 Redis 같은 In-Memory Store가 적합하다.

---

## 6. TTL을 이용한 비활성 사용자 처리

주변 친구 시스템에서는 오래된 위치가 계속 남아 있으면 문제가 된다.

예를 들어 사용자가 앱을 종료했는데 위치가 계속 남아 있으면 다른 친구에게

```text
A가 강남역 근처에 있음
```

이라고 계속 표시될 수 있다.

따라서 Location Cache에 TTL을 설정한다.

```text
SET user:123 location...
EXPIRE user:123 600
```

예를 들어 10분 동안 위치 업데이트가 없다면 자동으로 삭제한다.

그리고 새로운 위치가 들어올 때마다 TTL을 다시 갱신한다.

```text
위치 업데이트
     ↓
Location Update
     ↓
TTL Refresh
```

### 배운 점

TTL은 단순 캐시 최적화 기능만은 아니다.

이번 시스템에서는

> **사용자가 현재 활성 상태인지 판단하는 로직**

의 일부로 사용된다.

즉 Redis의 TTL 자체가 비즈니스 로직을 단순하게 만들어준다.

---

## 7. Redis Pub/Sub

이번 장에서 가장 중요한 부분이다.

한 사용자의 위치가 바뀌면 그 사용자의 친구들에게 위치 변경 사실을 전달해야 한다.

예를 들어

```text
A의 친구

B
C
D
E
```

가 있다면 A 위치 변경 시

```text
A 위치 업데이트
       ↓
A Channel Publish
       ↓
 ┌─────┼─────┐
 ▼     ▼     ▼
 B     C     D
```

처럼 전달할 수 있다.

이를 위해 Redis Pub/Sub을 사용한다.

사용자별 Channel을 만든다고 생각할 수 있다.

```text
user:A
user:B
user:C
...
```

B가 A의 친구라면 B가 연결되어 있는 WebSocket Server가

```text
SUBSCRIBE user:A
```

를 수행한다.

A 위치가 변경되면

```text
PUBLISH user:A {location}
```

한다.

그러면 A의 위치 변경을 구독하고 있는 WebSocket Server들이 메시지를 받을 수 있다.

---

## 8. 왜 Message Queue가 아니라 Pub/Sub인가?

위치 정보는 조금 유실되어도 괜찮다.

예를 들어

```text
10:00:00 위치 A
10:00:30 위치 B
10:01:00 위치 C
```

가 있는데 위치 B 이벤트 하나를 놓쳤다고 하더라도 곧 위치 C가 들어온다.

즉 과거 위치 이벤트를 반드시 다시 처리해야 하는 데이터가 아니다.

따라서

```text
메시지 영속성
ACK
Retry
Replay
```

등을 강하게 요구하는 시스템보다 Pub/Sub처럼 가벼운 구조가 적합하다.

### 배운 점

메시지 시스템을 선택할 때는

> Kafka가 더 강력한가?

보다는

> 이 이벤트를 잃어버렸을 때 문제가 되는가?

를 먼저 생각해야 한다.

주변 친구의 위치 이벤트는 **최신 값이 더 중요하고 중간 값은 상대적으로 중요하지 않다.**

그래서 Redis Pub/Sub이 요구사항에 잘 맞는다.

---

## 9. Location History는 별도로 저장

현재 위치와 위치 이력은 성격이 다르다.

### 현재 위치

```text
Redis Location Cache

user123 → 현재 위치
```

최신 값만 중요하다.

### 위치 이력

```text
Location History DB

user123
 ├─ 10:00 위치
 ├─ 10:00:30 위치
 ├─ 10:01 위치
 └─ ...
```

장기적으로 저장할 데이터이다.

향후

- 위치 패턴 분석
- 머신러닝
- 추천
- 통계

등에 사용할 수 있다.

### 배운 점

같은 "위치 데이터"라고 하더라도 사용 목적에 따라 저장소를 분리할 수 있다.

```text
실시간 조회용 데이터 → Redis

장기 보관/분석 데이터 → Persistent DB
```

즉 데이터의 **접근 패턴에 따라 저장소를 선택하는 것**이 중요하다.

---

## 10. 친구 추가/삭제는 어떻게 처리할까?

A와 B가 새로 친구가 되었다면 각 사용자의 WebSocket 연결을 담당하는 서버가 상대방의 위치 Channel을 구독해야 한다.

```text
A ↔ B 친구 추가

A WebSocket Server
        ↓
SUBSCRIBE user:B

B WebSocket Server
        ↓
SUBSCRIBE user:A
```

친구 관계가 삭제되면 반대로

```text
UNSUBSCRIBE
```

한다.

즉 친구 관계 변화는 단순히 DB의 Friend Table만 변경하는 것이 아니라 **실시간 메시지 구독 관계에도 반영되어야 한다.**

---

## 11. Redis Pub/Sub도 결국 Scale-out 해야 한다

사용자가 적다면 Redis 한 대로도 가능하지만 규모가 커지면 문제가 된다.

앞에서 계산한 것처럼 초당 수백만~수천만 개의 메시지를 처리해야 할 수 있기 때문이다.

따라서 Pub/Sub 서버 자체도 여러 서버로 분산한다.

예를 들어 User ID를 기준으로

```text
hash(userId)
       ↓
Redis Pub/Sub Node
```

를 결정할 수 있다.

문제는 Redis 서버를 추가하거나 제거할 때이다.

단순히

```text
hash(userId) % serverCount
```

를 사용하면 서버 수가 변경될 때 대부분의 사용자 매핑이 바뀐다.

그래서 **Consistent Hashing / Hash Ring**을 이용할 수 있다.

```text
          Redis A
            ●
        /       \
     User        Redis B
       ●           ●
        \         /
          Redis C
```

서버가 추가/삭제되더라도 일부 사용자만 다시 매핑하도록 한다.

### 배운 점

Consistent Hashing은 단순 캐시 분산에만 쓰이는 것이 아니라

> **어떤 사용자의 메시지를 어느 메시지 서버가 담당할 것인가**

와 같은 Routing 문제에도 사용할 수 있다.

---

## 12. Chapter 1 근접성 서비스와의 차이

이번 장에서 가장 기억에 남은 부분이다.

둘 다

> "내 주변에 무엇이 있는가?"

를 다루지만 문제의 성격이 다르다.

| 구분 | 근접성 서비스 | 주변 친구 |
|---|---|---|
| 대상 | 음식점/사업장 | 사용자 |
| 위치 변화 | 거의 없음 | 계속 이동 |
| 핵심 문제 | 공간 검색 | 실시간 위치 전파 |
| 대표 기술 | Geohash / Spatial Index | WebSocket / Pub/Sub |
| 데이터 성격 | Read 중심 | 지속적인 Update |
| 핵심 흐름 | Query | Event |

근접성 서비스는

```text
내 위치
   ↓
주변 객체 검색
```

문제라면,

주변 친구는

```text
친구 위치 변경
      ↓
Event 발생
      ↓
여러 친구에게 전파
```

문제에 더 가깝다.

그래서 처음 문제를 보고 단순히 Geohash부터 떠올리면 안 된다.

---

## 13. 오늘의 핵심 포인트

이번 장에서 가장 크게 배운 것은 **시스템 설계 문제의 겉모습만 보고 기술을 선택하면 안 된다는 것**이다.

"주변 친구"라는 이름 때문에 처음에는

```text
GPS
↓
Geohash
↓
주변 사용자 검색
```

구조를 먼저 생각하게 된다.

하지만 요구사항을 자세히 보면

```text
친구 위치가 계속 변경되고
        ↓
그 변경을 몇 초 안에
        ↓
여러 친구에게 전달해야 한다.
```

는 것이 핵심이다.

따라서 이 시스템의 본질은 **Geospatial Search보다 대규모 실시간 Message Fan-out 문제**에 가깝다.

이를 해결하기 위해

```text
WebSocket
    ↓
Redis Location Cache
    ↓
Redis Pub/Sub
    ↓
Consistent Hashing
```

등의 기술을 조합한다.
